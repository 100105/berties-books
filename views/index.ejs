<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Home - Bertie’s Bookshop</title>
  <style>
    body {
      font-family: "Poppins", Arial, sans-serif;
      background-color: #f9f5f2;
      margin: 0;
      padding: 0;
      text-align: center;
      color: #f9f5f2;
    }

    header {
      background-color: #d9b8a3;
      color: white;
      padding: 25px;
      letter-spacing: 1px;
    }

    h1 {
      margin: 0;
      font-size: 32px;
    }

    .nav-links {
      margin-top: 30px;
      display: flex;
      justify-content: center;
      gap: 20px;
    }

    a {
      color: #f9f5f2;
      font-weight: bold;
      text-decoration: none;
      background-color: #e9d4c7;
      padding: 10px 16px;
      border-radius: 6px;
    }

    a:hover {
      background-color: #d9b8a3;
      color: white;
    }

    .nav-link {
      display: inline-block;
      margin-top: 10px;
      background-color: #ffffff33;
      padding: 8px 12px;
      border-radius: 4px;
      color: white;
      font-weight: 600;
      text-decoration: none;
    }   

    .nav-link:hover {
      background-color: #ffffff55;
    }

  </style>
</head>

<body>
  <header>
    <h1>Welcome to Bertie’s Bookshop</h1>
    <% if (isLoggedIn) { %>
      <a href="/logout" class="nav-link">Logout</a>
    <% } %>
    <p><a href="/">Return to Home</a></p>
  </header>

  <div class="nav-links">
    <a href="/about">About</a>
    <a href="/books/search">Search Books</a>
    <a href="/weather/now">Weather</a>

    <% if (isLoggedIn) { %>
      <a href="/books/addbook">Add Book</a>
      <a href="/books/list">List All Books</a>
      <a href="/users/audit">Audit Log</a>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/users/login">Login</a>
      <a href="/users/register">Register</a>
    <% } %>
  </div>
</body>
</html>